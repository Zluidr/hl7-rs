# hl7-rs Plans.md

Created: 2026-07-30
Source: generated from root [`TODO.md`](TODO.md) (cross-crate/workspace-level tasks only — per-crate tasks stay in `crates/*/TODO.md`, per `TODO.md`'s own stated scope).

---

## Spec delta / Spec skip reason

**Spec skip reason:** This repo has no root `spec.md`. `Plans.md` is generated by mechanically restructuring the existing, already-authoritative root `TODO.md` into the task-ledger format — no new product, API, data-model, permission, billing, or distribution decisions are introduced beyond what `TODO.md`, `README.md`, and the per-crate `README.md`/`TODO.md` files already commit to. If a task below later needs a product-behavior decision that isn't already fixed in those documents (e.g. the exact `hl7-arrow` Arrow schema shape in HT-T10.1), a `spec.md` should be created at that point — noted inline on the task.

## Team validation

- `team_validation_mode`: `manual-pass` — this run reformats settled content rather than making new product/architecture/security decisions, so a single self-review across the five perspectives was used instead of spawning independent subagents.
- **Product**: scope matches `TODO.md`'s own stated boundary (root/workspace tasks only; per-crate detail stays in `crates/*/TODO.md`). No scope drift.
- **Architecture**: task dependencies preserved verbatim from `TODO.md` (e.g. `HT-T10.1 → HT-T10.2 → HT-T10.3 → HT-T30`); crate dependency direction (`satusehat → fhir-r4`, `hl7-mindray → hl7-v2`) already documented in `CLAUDE.md`, not duplicated here.
- **Security**: no secrets/`.env` reads required for this task; `T2.5` (security hardening) and `T0.3`/`deny.toml` (supply chain) carried through unchanged.
- **QA**: DoD lines below were tightened to be Yes/No-checkable where the source `TODO.md` only had a checkbox with no explicit pass condition (e.g. T1.1–T1.4, T2.x). Lint/format baseline already exists (`cargo fmt`, `cargo clippy -D warnings`, pre-commit hooks) — no setup task needed before implementation tasks.
- **Skeptic**: verified against actual repo state (not just `TODO.md`'s checkboxes) — `crates/hl7-mllp/benches/framing.rs` exists and `hl7-mllp`'s `CHANGELOG.md`/commit `d62c42b` confirm `bench_mllp_encode_decode` is implemented, so `T2.3` is marked `cc:wip`, not `cc:todo`, unlike its unchecked box in `TODO.md`.
- harness-mem / continuity check: no prior plan or handoff found for this repo (`.harness-mem/state/continuity.json` has `latest_handoff: null`, no `plans/manifest.json`) — nothing to reconcile against.

**DoD (Definition of Done):** a verifiable, Yes/No completion condition. "Works well" / "looks good" is not acceptable.
**Depends:** `-` (none), a task number (`HT-T10.1`), comma-separated (`T1.1, T1.2`), or a phase (`Phase 1`).

---

## v0.1 Scope — `hl7-arrow` crate

> **Goal:** ship a sibling crate that emits parsed HL7 v2 messages as Apache Arrow RecordBatches, so Rust-based HL7 ingestion can feed any Arrow-capable downstream (Python, TypeScript, R, Julia) without custom FFI or subprocess JSON round-tripping.
> **License impact:** none — `arrow-rs` is Apache-2.0, compatible with `deny.toml`'s allow-list; confirm transitive deps under HT-T10.3.
> **SemVer impact:** new sibling crate at its own `0.1.0`; no breaking changes to `hl7-mllp`, `hl7-v2`, `hl7-mindray`, `fhir-r4`, `satusehat` — `hl7-arrow` only consumes `hl7-v2` read-only.
> **Architectural posture:** one job, no runtime/transport opinion — emits RecordBatches, consumers choose the transport (stdout, file, socket, Flight, shared memory).

| Task | Description | DoD | Depends | Status |
|------|------|-----|---------|--------|
| HT-T01 | Audit workspace for existing Arrow emission capability | Finding recorded: no crate currently emits Arrow RecordBatches (confirmed by inspection of `crates/*` and `README.md`'s Crates table) | - | cc:done |
| HT-T10 | Add `hl7-arrow` crate | `crates/hl7-arrow/` exists; depends only on `hl7-v2` + `arrow-rs`, no other intra-workspace crate | HT-T01 | blocked |
| HT-T10.1 | Design Arrow schema for HL7 v2 messages [tdd:skip:schema-design-precedes-code] | `crates/hl7-arrow/README.md` published with schema-version matrix; RecordBatch schema defined for at least ORU^R01, covering type fidelity (TS/NM → Timestamp/Float64/Utf8), nullability/repetition (`List<T>`), segment grouping (struct/list columns), schema-version metadata field, and neutral HL7 field naming | HT-T10 | cc:todo |
| HT-T10.2 | Write Arrow emission examples: `mllp_listener.rs` (TCP MLLP → Arrow IPC on stdout) and `pyarrow_consumer.py` (reads the IPC stream) | Both examples run end-to-end on a fixture HL7 v2 message; round-trip verified (Rust writes → Python reads → same logical field values) | HT-T10.1 | blocked |
| HT-T10.3 | `hl7-arrow` crate metadata and publish readiness | `cargo publish --dry-run -p hl7-arrow` passes; crate has `description`/`repository`/`license`=Apache-2.0/`keywords`/`categories`; `#![forbid(unsafe_code)]` present; `cargo doc -p hl7-arrow` zero warnings; `arrow-rs` + transitive deps clear `deny.toml` | HT-T10.2 | blocked |
| HT-T20 | Document cross-language Arrow pattern in README | "Cross-language integration via Apache Arrow" section present in root `README.md` (pattern diagram, transport-neutral options, Rust+Python example, schema-versioning note) | HT-T10 (design level only) | cc:done |
| HT-T30 | First consumer feedback loop | `crates/hl7-arrow/CHANGELOG.md` has a `0.1.0` entry stating "validated end-to-end on \<date\>" after at least one external consumer confirms a real round-trip through their own HL7 pipeline | HT-T10.3 | blocked |

**v0.1 kill-criteria** (all must hold before `hl7-arrow` 0.1.0 is released — unchanged from `TODO.md`):
1. HT-T01 done. 2. HT-T10.1 done (schema + README). 3. HT-T10.2 done (both examples round-trip on a fixture message). 4. HT-T10.3 done (`cargo publish --dry-run` passes, `deny.toml` clean). 5. HT-T20 done. 6. `cargo test --workspace` includes a Rust-only Arrow round-trip test (encode → deserialize → assert field equality); Python round-trip stays in `examples/`, not CI. 7. `cargo-deny` clean for `arrow-rs` + transitive deps. 8. HT-T30 feedback received before the release note claims `validated`.

---

## Phase 0 — Foundation `[workspace]`

| Task | Description | DoD | Depends | Status |
|------|------|-----|---------|--------|
| T0.1 | Repository scaffolding: workspace `Cargo.toml`, `.gitignore`, `LICENSE` (Apache-2.0, "hl7-rs contributors"), `README.md`, `CONTRIBUTING.md` (incl. DCO sign-off), `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `SECURITY.md` | All listed files exist and are populated (verified present in repo root) | - | cc:done |
| T0.2 | CI baseline: `.github/workflows/ci.yml` (build/test/clippy/fmt on push+PR, `actions/checkout` pinned to SHA, `dtolnay/rust-toolchain@stable`, `actions/cache@v4`) plus an MSRV check job reading `rust-version` from the workspace manifest | `ci.yml` present with `test` and `msrv` jobs; both pass on `main` | T0.1 | cc:done |
| T0.3 | Dependency hygiene: `deny.toml` (license allow-list, advisories deny-all, duplicate-dep warn) + `cargo-audit` in CI with `audit.toml` for documented false-positives | `deny.toml` and `audit.toml` present and enforced in CI/pre-commit; when `hl7-arrow` lands (HT-T10), confirm `arrow-rs` + transitive deps clear `deny.toml` | T0.1 | cc:done |

---

## Phase 1 — Individual Crate v0.1.0

> Each crate ships `0.1.0` independently. Per-crate task detail lives in `crates/*/TODO.md`. `hl7-arrow` joins this phase as the sixth crate once HT-T10.1 lands.

| Task | Description | DoD | Depends | Status |
|------|------|-----|---------|--------|
| T1.1 | Publish readiness for all crates | `cargo publish --dry-run -p <crate>` passes for every crate (incl. `hl7-arrow` once it exists); each has `description`/`repository`/`license`/`keywords`/`categories`; `README.md` renders on docs.rs with no broken relative links; no intra-workspace `path =` deps remain (use `version =` only) | Phase 0 | cc:done |
| T1.2 | Documentation pass | `cargo doc --workspace --no-deps` zero warnings (incl. `hl7-arrow` once it exists); every public item has a doc comment; each crate's `lib.rs` includes `#![doc = include_str!("../README.md")]` | Phase 0 | cc:todo |
| T1.3 | Test coverage baseline | `cargo-tarpaulin` installed and run per crate; each crate (incl. `hl7-arrow`) at ≥ 70% coverage; coverage step (`cargo tarpaulin --workspace --out Xml`) added to CI and uploaded to Codecov or similar | Phase 0 | cc:todo |
| T1.4 | MSRV validation | `cargo +1.75.0 check --workspace` passes; `arrow-rs` MSRV compatibility with `1.75` verified once `hl7-arrow` lands (bump workspace MSRV with documented rationale, or pin a higher MSRV on `hl7-arrow` alone, if incompatible); MSRV bump policy (major/minor only) documented in `CONTRIBUTING.md` | Phase 0 | cc:todo |

---

## Phase 2 — Integration & Hardening `[0.x.0 → pre-1.0]`

| Task | Description | DoD | Depends | Status |
|------|------|-----|---------|--------|
| T2.1 | Workspace-root `tests/` integration suite: Mindray HL7 → FHIR R4 JSON → SATUSEHAT output (ePM series, BeneVision N-series PDS, iPM 9800 fixtures); negative tests for malformed MLLP frames and unknown 99MNDRY codes; `hl7-arrow` round-trip + repetition/`List<T>` negative test once HT-T10 lands | `tests/` directory exists at workspace root; all listed positive and negative cases pass in `cargo test --workspace` | Phase 1 | cc:todo |
| T2.2 | Fuzzing: `cargo-fuzz` targets `fuzz_mllp_decode`, `fuzz_hl7_parse`, and (once `hl7-arrow` lands) `fuzz_hl7_arrow_encode` | Each target runs 10M iterations clean before a major release; targets documented in `CONTRIBUTING.md` (nightly-only, run outside default CI) | Phase 1 | cc:todo |
| T2.3 | Benchmarks via `criterion`: `bench_mllp_encode_decode` (MB/s), `bench_hl7_parse` (msg/s for ORU^R01), `bench_mindray_extract`, and (once `hl7-arrow` lands) `bench_hl7_arrow_encode` (target ≥ 10,000 msg/s on commodity hardware for ORU^R01, revisit after first measurement); CI job comparing against `main` | `crates/hl7-mllp/benches/framing.rs` (`bench_mllp_encode_decode`, commit `d62c42b`), `crates/hl7-v2/benches/parse.rs` (`bench_hl7_parse` — `parse/oru_r01`, `segment_lookup/segments_obx`), and `crates/hl7-mindray/benches/extract.rs` (`bench_mindray_extract` — `extract/from_message`, `end_to_end/parse_and_extract`) all compile and run under `cargo bench --workspace`; `.github/workflows/ci.yml` has a `benchmarks` job that runs on PRs, benchmarks `origin/main` via `git worktree` as a Criterion baseline, then compares the PR branch against it and uploads the report artifact. `hl7-arrow`'s `bench_hl7_arrow_encode` still pending HT-T10. | Phase 1 | cc:done |
| T2.4 | SemVer compatibility: `cargo-semver-checks` in CI on every crate-changing PR, blocking merge on an undeclared breaking change; separate schema-hash diff check for `hl7-arrow`'s Arrow schema (tracked independently of crate SemVer) once it lands | `cargo-semver-checks` runs in CI and blocks on breaking changes without a major bump; `hl7-arrow` schema-hash diff check added once the crate exists | Phase 1 | cc:todo |
| T2.5 | Security hardening: weekly scheduled `cargo audit` via GitHub Actions `schedule:`; `cargo-deny` CI check for license/advisory violations; audit of all `unwrap()`/`expect()` in non-test code (replace with proper errors); confirm `#![forbid(unsafe_code)]` in every crate root (incl. `hl7-arrow`'s public surface once it lands, independent of `arrow-rs`'s internal unsafe use) | Weekly `cargo audit` scheduled workflow exists; `cargo-deny` check active in CI; `unwrap()`/`expect()` audit complete with findings resolved or explicitly justified; `#![forbid(unsafe_code)]` confirmed present in every crate's `lib.rs` | Phase 1 | cc:todo |

---

## Phase 3 — Stable Release `[1.0.0]`

| Task | Description | DoD | Depends | Status |
|------|------|-----|---------|--------|
| T3.1 | Pre-1.0 audit: external security review of the public API surface; API stability review (no planned post-1.0 breaking changes); full documentation pass (every public item, every error variant); CHANGELOGs complete `0.0.1` → `1.0.0` per crate (`hl7-arrow`'s covers `0.1.0` → `1.0.0`) | External security review recorded; documentation pass complete with no gaps; every crate's `CHANGELOG.md` has an entry for every released version through `1.0.0` | Phase 2 | cc:todo |
| T3.2 | Release tooling: `cargo-release` config (`.cargo/release.toml`) with release order `hl7-mllp → hl7-v2 → hl7-mindray → hl7-arrow → fhir-r4 → satusehat` (waiting ~1 min after `hl7-v2`/`fhir-r4` for crates.io indexing), auto-tagging `<crate>-v<version>`, auto-updated workspace dep versions; GitHub Release workflow triggered on `<crate>-v*` tag push with auto-generated notes (including Arrow schema version(s) for `hl7-arrow` releases); crates.io publish token scoped per-crate | `.cargo/release.toml` present with the documented release order; tag-push workflow publishes a GitHub Release with auto-generated notes; publish tokens verified scoped per-crate, not workspace-wide | Phase 2 | cc:todo |
| T3.3 | Post-release: announce on `r/rust` and `users.rust-lang.org`; open issues for next-cycle features; archive completed `TODO.md` items into `CHANGELOG.md` | Announcement posts made; next-cycle issues filed; `TODO.md` items completed as of `1.0.0` are archived into `CHANGELOG.md`, not silently deleted | T3.2 | cc:todo |

---

## Toolchain reference

| Tool | Purpose | Install |
|---|---|---|
| `cargo-audit` | CVE scanning | `cargo install cargo-audit` |
| `cargo-deny` | License + advisory policy | `cargo install cargo-deny` |
| `cargo-tarpaulin` | Coverage (Linux only) | `cargo install cargo-tarpaulin` |
| `cargo-semver-checks` | Breaking-change detection | `cargo install cargo-semver-checks` |
| `cargo-release` | Automated release workflow | `cargo install cargo-release` |
| `cargo-fuzz` | Fuzz testing (requires nightly) | `cargo install cargo-fuzz` |
| `cargo-machete` | Unused dependency detection | `cargo install cargo-machete` |
| `cargo-skill` *(v0.1)* | Skill discovery/packaging for Cargo projects | `cargo install cargo-skill` |

## Version log

| Version | Date | Description |
|---|---|---|
| v0.1 | 2026-07-30 | `Plans.md` generated from root `TODO.md`. All workspace-level tasks (`hl7-arrow` v0.1 scope, Phases 0–3) carried over with English-only `cc:` markers. `T2.3` upgraded from `TODO.md`'s unchecked box to `cc:wip` after confirming `bench_mllp_encode_decode` is already implemented (commit `d62c42b`). Per-crate task detail intentionally left in `crates/*/TODO.md`, matching `TODO.md`'s own stated scope. |
| v0.1.1 | 2026-07-30 | `T2.3` moved `cc:wip` → `cc:done`. Added `bench_hl7_parse` (`crates/hl7-v2/benches/parse.rs`) and `bench_mindray_extract` (`crates/hl7-mindray/benches/extract.rs`), both with a `criterion` dev-dependency and `[[bench]]` target; added a `benchmarks` job to `.github/workflows/ci.yml` that runs on PRs and compares against a `main`-branch Criterion baseline via `git worktree`. Verified: `cargo bench --workspace --no-run` compiles all three bench targets, both new benches pass in `--test` smoke mode, `cargo clippy -p hl7-v2 -p hl7-mindray --all-targets --all-features -D warnings` is clean, `cargo test --workspace --all-features` passes (63 passed, 3 ignored). Note: `cargo clippy --workspace --all-targets --all-features -D warnings` still fails on 2 pre-existing, unrelated findings in `crates/hl7-mllp/src/lib.rs` (confirmed present on `main` before this change, out of scope for T2.3). |

**Update discipline:** when a task's status changes, append a date to this Version log. When a task is removed, tombstone it here — no silent deletions.
